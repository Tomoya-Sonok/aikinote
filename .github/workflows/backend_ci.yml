name: Backend CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/**'
      - '.github/workflows/backend_ci.yml'
      # Uncomment the following lines if you want to ignore certain paths
      # paths-ignore:
      #   - 'docs/**'
      #   - 'README.md'

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # package.jsonのpackageManagerフィールドを読むので、このステップは不要
      # - name: Enable Corepack
      #   run: corepack enable

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          # pnpmのロックファイルへのパスを指定
          cache-dependency-path: backend/pnpm-lock.yaml
          # package.jsonのpackageManagerフィールドを読むので、この行も不要
          # pnpm-version: 8 

      - name: Install Backend Dependencies
        working-directory: ./backend
        run: pnpm install --frozen-lockfile

      - name: Build Backend
        working-directory: ./backend
        run: pnpm build

      # - name: Run Backend Tests
      #   run: pnpm test
      #   working-directory: ./backend
